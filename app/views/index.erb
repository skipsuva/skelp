<br>
<h2>Featured Bar</h2>

<h1><a href="/bars/<%=@bar.slug%>"><%= @bar.name %></a></h1>


<a href="/bars/<%=@bar.slug%>"><img src="<%= @bar.img_url %>" alt="<%= @bar.name %> image" /></a>

<h5>Located: <%= @bar.address %> </h5>
<h5>Theme: <%= @bar.theme %> </h5>
<h5>Average Rating: <%= Review.all.where(bar_id: @bar.id).average(:rating).to_i != 0 ? Review.all.where(bar_id: @bar.id).average(:rating).to_i : "No Ratings Yet" %> </h5>

<br>
<!-- <h3>Reviews:</h3>

<% Review.all.where(bar_id: @bar.id).each do |review| %>
  <ul>
    <h5>Rating: <%= review.rating %>/5</h5>
    <h6> <a href="/patrons/<%=Patron.find_by_id(review.patron_id).slug%>"> <%= Patron.find_by_id(review.patron_id).name%></a></h6>
    <p><%= review.comment %></p>
    <h5>Recommended? <%= review.would_recommend == true ? "Yes" : "No" %></h5>
  </ul>
  <br>
  <%end%> -->
